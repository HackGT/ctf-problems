Build the image with:
    docker build -t pycache ./

And run it with:
    docker run -p 9191:9191 -t pycache

You can test the challenge with:
    python easy_solution.py
    python fast_solution.py

The competitors should be given cache.py and README.txt.
